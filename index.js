ReactDOM.render(<h1>Hello, everyone!</h1>, document.getElementById("root"))
// ReactDOM.render() is used to render elements.

/* The first parameter is what do I want to in the screen,
and the second parameter is where do I want it to be.
*/

/* Under the hood what react is doing is that it's taking the dom element, 
we age grabing the getElementById, and its runnig something similar to .append / 
appendChild to it. In our case its appending our paragraph. 
*/